<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xmlns:context="http://www.springframework.org/schema/context" 
       xmlns:jee="http://www.springframework.org/schema/jee" 
       xmlns:cache="http://www.springframework.org/schema/cache" 
       xmlns:p="http://www.springframework.org/schema/p" 
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd 
                        http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd
			http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd 
			http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee.xsd 
			http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">
    
    <bean id="StorageManager" class="org.entando.entando.plugins.jpcds.aps.system.storage.CdsStorageManager">
        <property name="baseURL" ><value>${resourceRootURL}</value></property>
        <property name="baseDiskRoot" ><value>${resourceDiskRootFolder}</value></property>
        <property name="protectedBaseDiskRoot" ><value>${protectedResourceDiskRootFolder}</value></property>
        <property name="protectedBaseURL" ><value>${protectedResourceRootURL}</value></property>
        <property name="enabled" value="${cds.enabled:false}" />
        <property name="cdsPublicUrl" value="${cds.public.url:https://cds.entando.org}" />
        <property name="cdpPrivateUrl" value="${cds.private.url:https://cds.entando.org}" />
        <property name="cdsPath" value="${cds.path:/api/v1}" />
        <property name="kcAuthUrl" value="${keycloak.auth.url:http://localhost:8081/auth}" />
        <property name="kcRealm" value="${keycloak.realm:entando}" />
        <property name="kcClientId" value="${keycloak.client.id:entando-app}" />
        <property name="kcClientSecret" value="${keycloak.client.secret:}" />
    </bean>
    
</beans>
