<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="org.entando.entando.keycloak.services"/>

    <bean id="keycloakConfiguration"
          class="org.entando.entando.keycloak.services.KeycloakConfiguration">
        <property name="authUrl">
            <value>${keycloak.authUrl}</value>
        </property>
        <property name="realm">
            <value>${keycloak.realm}</value>
        </property>
        <property name="clientId">
            <value>${keycloak.clientId}</value>
        </property>
        <property name="clientSecret">
            <value>${keycloak.clientSecret}</value>
        </property>
    </bean>

    <bean id="keycloakService" class="org.entando.entando.keycloak.services.KeycloakService">
        <constructor-arg index="0" ref="keycloakConfiguration"/>
    </bean>

    <bean id="AuthenticationProviderManager" primary="true" class="org.entando.entando.keycloak.services.AuthenticationProviderManager"/>
    <bean id="UserManager" primary="true" class="org.entando.entando.keycloak.services.UserManager"/>

</beans>
